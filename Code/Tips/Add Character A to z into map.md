
```java
     for(char i = 'a'; i<='z'; i++){

            map.put(i,map.getOrDefault(i,0)+1);

        }

        for(char i = 'A'; i<='Z'; i++){

            map.put(i,map.getOrDefault(i,0)+1);

        }
```

